{
  "common": {
    "boolean": {
      "true": "true",
      "false": "false"
    }
  },
  "layout": {
    "language": {
      "label": "Language",
      "ariaLabel": "Language selector",
      "english": "English",
      "portuguese": "Portuguese"
    },
    "sidebar": {
      "navigationAriaLabel": "Section navigation",
      "title": "Performance<br />Patterns",
      "description": "Anti-patterns & best practices for modern Angular apps.",
      "techStack": "Zone.js - Signals - RxJS 7",
      "creditPrefix": "developed by"
    }
  },
  "sections": {
    "getterVsSignal": {
      "title": "Getter vs Signal",
      "navSubtitle": "Change detection cost",
      "tag": "Computed",
      "description": "A class getter is re-evaluated on <strong>every change detection cycle</strong> regardless of whether its dependencies changed. A <code>computed()</code> signal memoizes the result and only recalculates when a tracked signal dependency changes.",
      "panels": {
        "anti": "Anti-pattern",
        "best": "Best practice"
      },
      "demo": {
        "label": "Live demo",
        "toggleFilterTag": "Toggle \"filter\" tag",
        "triggerChangeDetection": "Trigger change detection",
        "clear": "Clear",
        "stats": {
          "cdTriggers": "CD triggers",
          "getterCallCount": "Getter call count",
          "computedReevaluations": "Computed re-evaluations",
          "getterResult": "Getter result",
          "computedResult": "Computed result"
        },
        "consoleTitle": "Console output",
        "consoleEmpty": "Click \"Trigger change detection\" to see the difference...",
        "tip": "Each CD trigger increments the getter call count. The computed re-evaluation count only increases when you \"Toggle filter tag\" - regardless of how many CD cycles occurred.",
        "logs": {
          "getterPrefix": "[GETTER] CD cycle -> re-evaluated ->",
          "computedPrefix": "[COMPUTED] filterTags changed -> re-evaluated ->"
        }
      }
    },
    "lazyLoading": {
      "title": "Lazy Loading",
      "navSubtitle": "Bundle strategy",
      "tag": "Bundle size",
      "description": "<code>loadChildren</code> with NgModules requires shipping an entire module graph per route. <code>loadComponent</code> with standalone components produces a minimal, targeted chunk containing only that component and its direct imports - nothing more.",
      "panels": {
        "anti": "Anti-pattern - NgModule",
        "best": "Best practice - Standalone"
      },
      "insight": {
        "title": "Why it matters",
        "description": "An NgModule chunk includes every component declared in that module, whether the route uses them or not. Standalone lazy-loading lets the bundler apply tree-shaking at the component level - the chunk only contains what's directly imported by <code>DashboardComponent</code>. Typical savings: <span class='highlight'>20 - 60 KB</span> per route for mid-size modules."
      }
    },
    "signalReadonly": {
      "title": "Signal Encapsulation",
      "navSubtitle": "Writable vs Readonly",
      "tag": "Encapsulation",
      "description": "Exposing a <code>WritableSignal</code> publicly breaks encapsulation - any consumer can mutate state the service is supposed to own. Use <code>.asReadonly()</code> to expose a view-only projection while keeping mutation methods private to the service.",
      "panels": {
        "anti": "Anti-pattern",
        "best": "Best practice"
      },
      "insight": {
        "title": "Rule of thumb",
        "description": "Private <code>_signal = signal()</code> inside the service -> public <code>signal = this._signal.asReadonly()</code> for consumers. This is the same principle as a private setter with a public getter in traditional OOP, but enforced at the type level by Angular's signal API."
      }
    },
    "serviceScope": {
      "title": "Service Scope",
      "navSubtitle": "Root vs component-level",
      "tag": "DI",
      "description": "<code>providedIn: 'root'</code> creates a singleton that lives for the entire app lifetime. For ephemeral state - like a multi-step wizard - use component-level providers so the service is created fresh when the component mounts and garbage-collected when it is destroyed.",
      "panels": {
        "anti": "Anti-pattern - global singleton",
        "best": "Best practice - component-scoped"
      },
      "insight": {
        "title": "Lifecycle alignment",
        "description": "Component-scoped providers are tied to the component's destroy lifecycle. This means no manual reset logic, no stale state between wizard sessions, and automatic memory reclamation - Angular handles it for you. All child components of the wizard also receive the same scoped instance."
      }
    },
    "inputDebounce": {
      "title": "Input Debounce",
      "navSubtitle": "RxJS operators",
      "tag": "RxJS",
      "description": "Without <code>debounceTime</code>, every keystroke fires a network request. With <code>debounceTime(300) + distinctUntilChanged()</code>, requests only fire after the user pauses - and only when the value actually changed.",
      "panels": {
        "anti": "Anti-pattern",
        "best": "Best practice"
      },
      "demo": {
        "label": "Live demo - type something",
        "reset": "Reset",
        "placeholder": "Search users...",
        "stats": {
          "withoutDebounce": "X Without debounce - API calls fired",
          "withDebounce": "OK With debounce - API calls fired"
        },
        "logKinds": {
          "immediate": "IMMEDIATE",
          "debounced": "DEBOUNCED"
        },
        "empty": "Start typing to see requests logged here..."
      }
    },
    "trackFor": {
      "title": "track in @for",
      "navSubtitle": "DOM reconciliation",
      "tag": "Performance",
      "description": "Without a proper <code>track</code> expression, Angular cannot identify which items changed and re-processes <strong>all N views</strong> on every list mutation. With <code>track item.id</code>, only genuinely new or updated items trigger DOM mutations.",
      "panels": {
        "anti": "track $index",
        "best": "track item.id"
      },
      "demo": {
        "label": "Live demo - watch the view re-process counter",
        "reset": "Reset",
        "domViewsProcessed": "DOM views processed",
        "reprocessedTitle": "times re-processed",
        "tip": "<strong>xN</strong> shows how many times each view was re-processed by Angular. On the <em>track $index</em> side, all items increment on every operation - even unchanged ones. On the <em>track item.id</em> side, only the new item starts at x1; existing items stay untouched.",
        "bad": {
          "label": "track $index",
          "prepend": "+ Prepend",
          "shuffle": "Shuffle"
        },
        "good": {
          "label": "track item.id",
          "prepend": "+ Prepend",
          "shuffle": "Shuffle"
        },
        "items": {
          "designTokens": "Design tokens",
          "authMiddleware": "Auth middleware",
          "apiRateLimiting": "API rate limiting",
          "unitTestCoverage": "Unit test coverage",
          "dockerSetup": "Docker setup",
          "generatedTask": "Task #{{id}}"
        }
      },
      "insight": {
        "title": "When track $index is acceptable",
        "description": "If the list is <strong>order-stable</strong> (never reordered, items only appended at the end, never removed from the middle) and items carry no internal DOM state (inputs, animations), <code>$index</code> is fine. In any other scenario - shuffle, prepend, arbitrary removal, pagination - prefer <code>track item.id</code> or another stable identity property."
      }
    },
    "observableCleanup": {
      "title": "Observable Cleanup",
      "navSubtitle": "Subscription management",
      "tag": "RxJS",
      "description": "Subscriptions that outlive their component cause memory leaks and ghost event handlers. Angular 16+ ships <code>takeUntilDestroyed()</code> as the canonical solution - no boilerplate Subject, no <code>ngOnDestroy</code> required.",
      "panels": {
        "anti": "Manual unsubscribe",
        "antiNote": "Verbose. Easy to forget when there are multiple subscriptions.",
        "middle": "takeUntil pattern",
        "middleNote": "Better - but still requires a Subject and a full ngOnDestroy lifecycle hook.",
        "best": "takeUntilDestroyed",
        "bestNote": "Zero boilerplate. Works in constructors and inject() context. Angular 16+."
      },
      "insight": {
        "title": "Key advantage of takeUntilDestroyed",
        "description": "When called inside the constructor (or any function in the inject context), it automatically binds to the current component's <code>DestroyRef</code> without needing to pass it explicitly. You can also pass a <code>destroyRef</code> explicitly when calling from outside the inject context - for example, a standalone service method."
      },
      "warning": {
        "title": "HttpClient requests do not need cleanup",
        "description": "Observables returned by <code>HttpClient</code> are finite - they emit a single response and then complete automatically, releasing all resources without memory leaks. Applying <code>takeUntilDestroyed</code> to them is harmless, but unnecessary. Reserve cleanup for long-lived streams: <code>interval()</code>, <code>fromEvent()</code>, WebSocket connections, and manually managed <code>Subject</code>s."
      }
    },
    "sharedModulePattern": {
      "title": "SharedModule Anti-pattern",
      "navSubtitle": "Modern standalone approach",
      "tag": "Architecture",
      "description": "The <code>SharedModule</code> pattern was a pragmatic solution in the NgModule world - declare once, export everywhere. With standalone components it becomes a liability: it forces every consumer to load components they never use, bloats chunk sizes, and creates invisible coupling through implicit re-exports.",
      "panels": {
        "anti": "SharedModule",
        "best": "Direct standalone imports"
      },
      "insights": {
        "anti": {
          "title": "SharedModule problems",
          "items": {
            "item1": "All declared components included in every consumer's bundle",
            "item2": "Implicit dependency - hard to know what a module actually needs",
            "item3": "Single point of coupling - touching SharedModule risks regressions everywhere",
            "item4": "Prevents effective tree-shaking by the bundler"
          }
        },
        "best": {
          "title": "Standalone benefits",
          "items": {
            "item1": "Each component declares exactly what it needs - nothing more",
            "item2": "Bundler can tree-shake at the component level",
            "item3": "Dependencies are explicit and colocated with the component",
            "item4": "Smaller chunks, faster lazy loading, easier refactoring"
          }
        }
      }
    },
    "deferBlocks": {
      "title": "@defer Blocks",
      "navSubtitle": "Lazy loading in the template",
      "tag": "Angular 17+",
      "description": "Eagerly loaded components bloat the initial bundle even when the user never scrolls to them. <code>@defer</code> performs <strong>automatic code-splitting</strong> for the deferred component - no bundler config, no <code>loadComponent</code>, just a template declaration.",
      "panels": {
        "anti": "Eager loading",
        "best": "@defer with helper blocks",
        "triggers": "All available triggers"
      },
      "insight": {
        "title": "Automatic code-splitting",
        "description": "The Angular CLI detects components inside <code>@defer</code> and automatically moves them into separate chunks at build time. No extra configuration needed - the same mechanism used by <code>loadComponent</code> in routes, but applicable to any part of the template. The <code>@placeholder</code> block renders immediately; the main content is downloaded and rendered only when the trigger fires."
      },
      "warning": {
        "title": "When not to use @defer",
        "description": "Components above the fold, critical for LCP (Largest Contentful Paint), or that require immediate SSR hydration <strong>should not be deferred</strong>. Reserve <code>@defer</code> for secondary content: charts, rich-text editors, data grids, inactive tabs, and anything below the initial visible screen."
      }
    },
    "onPush": {
      "title": "OnPush Change Detection",
      "navSubtitle": "Change detection strategy",
      "tag": "Performance",
      "description": "With the default strategy (<code>Default</code>), Angular checks the template of <strong>every component</strong> on each change detection cycle - even when nothing relevant changed. <code>OnPush</code> puts the component to sleep: it only re-checks when an <code>@Input</code> reference changes, a Signal emits, or the <code>async</code> pipe receives a new value.",
      "panels": {
        "anti": "ChangeDetectionStrategy.Default",
        "best": "ChangeDetectionStrategy.OnPush"
      },
      "demo": {
        "label": "Live demo - observe when each strategy re-renders",
        "triggerExternalCd": "Trigger external CD",
        "changeSignal": "Change Signal / @Input",
        "reset": "Reset",
        "stats": {
          "defaultRenders": "Default - renders",
          "onPushRenders": "OnPush - renders",
          "signalVersion": "Signal version"
        },
        "legend": {
          "externalCd": "<strong>Trigger external CD</strong> - simulates setTimeout, HTTP response, parent event. Default re-renders. OnPush <em>does not re-render</em>.",
          "signalChange": "<strong>Change Signal / @Input</strong> - both strategies re-render, because Angular knows the relevant state changed."
        },
        "tip": "Press <strong>Trigger external CD</strong> several times and notice OnPush renders stays at zero - the component literally skips all binding checks."
      },
      "insight": {
        "title": "OnPush + Signals = the ideal combination",
        "description": "With Signals, Angular knows exactly <em>which</em> node in the template changed and updates only that DOM node, without traversing the entire component tree. Declaring <code>OnPush</code> formalizes this in the component's CD strategy, eliminating unnecessary checks even in Zone.js apps. In components built exclusively on Signals, Angular can eventually skip CD entirely (Zoneless Architecture - Angular 18+)."
      }
    },
    "ngOptimizedImage": {
      "title": "NgOptimizedImage",
      "navSubtitle": "LCP & lazy loading",
      "tag": "Performance",
      "description": "A plain <code>&lt;img src&gt;</code> has no lazy loading, no intrinsic dimensions, and no fetch-priority hint — all of which hurt <strong>LCP</strong> and cause layout shift. The <code>NgOptimizedImage</code> directive fixes all three automatically.",
      "panels": {
        "anti": "Plain img tag",
        "best": "NgOptimizedImage directive"
      },
      "demo": {
        "label": "Live demo — inspect generated attributes in DevTools → Elements",
        "plainLabel": "plain &lt;img&gt;",
        "optimizedLabel": "NgOptimizedImage",
        "tip": "Open DevTools → Elements and compare both sides. The plain images lack <code>loading</code>, <code>width/height</code>, and <code>fetchpriority</code>. The optimized images have <code>loading=\"lazy\"</code> on below-fold images, <code>fetchpriority=\"high\"</code> on the priority one (preventing layout shift and improving LCP)."
      }
    },
    "virtualScroll": {
      "title": "Virtual Scroll",
      "navSubtitle": "Large list rendering",
      "tag": "Performance",
      "description": "Rendering thousands of items in the DOM at once causes massive layout thrashing and high memory usage. CDK <code>cdk-virtual-scroll-viewport</code> keeps only the visible rows in the DOM — list size no longer affects performance.",
      "panels": {
        "anti": "All items in DOM",
        "best": "CDK Virtual Scroll"
      },
      "demo": {
        "label": "Live demo — scroll both lists and compare",
        "antiLabel": "all items rendered",
        "bestLabel": "1 000 items",
        "inDom": "in DOM",
        "tip": "Open DevTools → Elements and count list rows on each side. The left renders 200 nodes eagerly (a real app with 1 000 would lock the browser). The CDK side renders only ~10 DOM nodes regardless of list length — scroll to item #1 000 instantly with no performance cost."
      }
    },
    "purePipe": {
      "title": "Pure Pipe",
      "navSubtitle": "Memoized transforms",
      "tag": "Performance",
      "description": "Calling a method directly in a template expression re-executes it on <strong>every change detection cycle</strong>. A <code>pure: true</code> pipe is memoized by Angular — it only re-runs when the input reference changes.",
      "panels": {
        "anti": "Method call in template",
        "best": "Pure pipe"
      },
      "demo": {
        "label": "Live demo — watch call counts",
        "triggerCD": "Trigger CD",
        "changePrice": "Change price",
        "reset": "Reset",
        "stats": {
          "cdTriggers": "CD triggers",
          "methodCalls": "Method calls",
          "pipeCalls": "Pipe calls"
        },
        "antiLabel": "method call",
        "bestLabel": "pure pipe",
        "tip": "Press <strong>Trigger CD</strong>: the method is called 5 times (once per product per cycle) while the pipe call count stays at zero — prices did not change so Angular skips re-evaluation. Press <strong>Change price</strong>: only 1 new pipe call fires (the changed item). The method always fires 5 times."
      }
    },
    "toSignal": {
      "title": "toSignal()",
      "navSubtitle": "Observable → Signal bridge",
      "tag": "Signals",
      "description": "<code>toSignal()</code> converts any Observable into a readable Signal. It automatically subscribes in the injection context and unsubscribes when the component is destroyed — eliminating manual subscription boilerplate.",
      "panels": {
        "anti": "Manual subscribe()",
        "best": "toSignal()"
      }
    },
    "effectVsComputed": {
      "title": "effect() vs computed()",
      "navSubtitle": "Signals — right tool",
      "tag": "Signals",
      "description": "Use <code>computed()</code> to derive values from signals — it is synchronous, memoized, and tracked. Use <code>effect()</code> only for side-effects such as logging, analytics, or DOM manipulation. Writing to a signal inside <code>effect()</code> to compute a value is an anti-pattern.",
      "panels": {
        "anti": "effect() to derive state",
        "best": "computed() + effect() for side-effects"
      },
      "demo": {
        "label": "Live demo — observe evaluation order",
        "changeFirstName": "Change first name",
        "changeLastName": "Change last name",
        "clear": "Clear",
        "stats": {
          "renders": "Renders",
          "effectRuns": "Effect runs",
          "computedEvals": "Computed evaluations"
        },
        "effectNote": "Updated asynchronously after render — causes an extra render cycle",
        "computedNote": "Derived synchronously during the same render — zero extra cycles",
        "consoleTitle": "Evaluation log",
        "consoleEmpty": "Change a name to observe evaluation order...",
        "tip": "Each name change: <code>computed()</code> evaluates once, synchronously, during rendering. The <code>effect()</code> runs <em>after</em> the render and writes to another signal — causing Angular to schedule a second render. Watch the <strong>Renders</strong> counter climb roughly twice as fast as <strong>Computed evaluations</strong>."
      }
    },
    "linkedSignal": {
      "title": "linkedSignal()",
      "navSubtitle": "Derived writable signal",
      "tag": "Signals",
      "description": "<code>linkedSignal()</code> creates a signal that derives its default from another signal but remains writable. When the source changes, it resets to the new derived value — eliminating the verbose pattern of a separate signal plus an <code>effect()</code> to sync them.",
      "panels": {
        "anti": "Signal + effect() to sync",
        "best": "linkedSignal()"
      }
    },
    "signalInput": {
      "title": "Signal Input",
      "navSubtitle": "Reactive @Input",
      "tag": "Signals",
      "description": "The <code>@Input()</code> decorator is not reactive — you need <code>ngOnChanges</code> to react to changes. The <code>input()</code> function returns a signal, making the input directly composable with <code>computed()</code> and <code>effect()</code> without any lifecycle hook.",
      "panels": {
        "anti": "@Input() decorator",
        "best": "input() signal function"
      }
    },
    "facadePattern": {
      "title": "Facade Pattern",
      "navSubtitle": "Simplified service API",
      "tag": "Architecture",
      "description": "A component that injects multiple unrelated services becomes tightly coupled to every one of them. A <strong>Facade</strong> service aggregates and orchestrates those services internally — the component depends on a single, stable API.",
      "panels": {
        "anti": "Multiple injected services",
        "best": "Single facade injection"
      }
    },
    "smartDumb": {
      "title": "Smart / Dumb Components",
      "navSubtitle": "Container vs presentational",
      "tag": "Architecture",
      "description": "<strong>Smart</strong> (container) components fetch data and manage state. <strong>Dumb</strong> (presentational) components receive data via <code>input()</code> and emit events via <code>output()</code> — they are stateless, easily testable, and reusable.",
      "panels": {
        "anti": "Dumb component fetches its own data",
        "best": "Smart fetches, dumb renders"
      }
    },
    "injectionToken": {
      "title": "InjectionToken",
      "navSubtitle": "Testable configuration",
      "tag": "Architecture",
      "description": "Hardcoding <code>environment.apiUrl</code> directly in a service creates an untestable, unswappable dependency. An <code>InjectionToken</code> makes the value part of Angular's DI graph — trivially overridable in tests and per-environment configurations.",
      "panels": {
        "anti": "Hardcoded environment reference",
        "best": "InjectionToken"
      }
    },
    "folderStructure": {
      "title": "Folder Structure",
      "navSubtitle": "Feature vs type-based",
      "tag": "Architecture",
      "description": "Organizing by <strong>type</strong> (<code>components/</code>, <code>services/</code>, <code>pipes/</code>) mixes unrelated features and makes it hard to reason about or delete a feature. Organizing by <strong>feature</strong> keeps everything cohesive — each folder is self-contained and independently removable.",
      "panels": {
        "anti": "Type-based structure",
        "best": "Feature-based structure"
      }
    },
    "bypassSecurity": {
      "title": "bypassSecurityTrust",
      "navSubtitle": "XSS prevention",
      "tag": "Security",
      "description": "Calling <code>bypassSecurityTrustHtml(userInput)</code> disables Angular's built-in XSS sanitizer. Use <code>[innerHTML]</code> directly for user-provided content — Angular sanitizes it automatically. Only bypass sanitization for HTML you generate and control on the server.",
      "panels": {
        "anti": "bypassSecurityTrustHtml(userInput)",
        "best": "[innerHTML] with auto-sanitization"
      }
    },
    "httpOnlyCookie": {
      "title": "HttpOnly Cookie",
      "navSubtitle": "JWT storage security",
      "tag": "Security",
      "description": "Storing a JWT in <code>localStorage</code> makes it accessible to any JavaScript running on the page — including XSS payloads. An <strong>HttpOnly</strong> cookie is invisible to JavaScript entirely, eliminating the most common token-theft attack vector.",
      "panels": {
        "anti": "localStorage.setItem('token', jwt)",
        "best": "Set-Cookie: HttpOnly; Secure; SameSite=Strict"
      }
    },
    "httpClientGenerics": {
      "title": "HttpClient Generics",
      "navSubtitle": "Typed HTTP responses",
      "tag": "TypeScript",
      "description": "Calling <code>http.get()</code> without a type parameter returns <code>Observable&lt;Object&gt;</code> — all type safety is lost and consumers must cast manually. Providing the generic <code>http.get&lt;User[]&gt;()</code> gives you full IntelliSense, compile-time checks, and safe refactoring.",
      "panels": {
        "anti": "http.get() — untyped",
        "best": "http.get<T>() — typed"
      }
    },
    "outputFn": {
      "title": "output() Function",
      "navSubtitle": "Modern event emitter",
      "tag": "TypeScript",
      "description": "The <code>@Output()</code> decorator with <code>EventEmitter</code> is the classic approach but is verbose and class-based. The <code>output()</code> function is the modern equivalent — no decorator, no import of <code>EventEmitter</code>, and it pairs naturally with signal inputs.",
      "panels": {
        "anti": "@Output() EventEmitter",
        "best": "output() function"
      }
    },
    "typeGuard": {
      "title": "Type Guards",
      "navSubtitle": "unknown over any",
      "tag": "TypeScript",
      "description": "Using <code>any</code> silences the TypeScript compiler entirely — property access is unchecked and runtime crashes go undetected at compile time. <code>unknown</code> forces you to narrow the type before use, keeping the type system engaged and your code safe.",
      "panels": {
        "anti": "function process(value: any)",
        "best": "unknown + type guard narrowing"
      }
    },
    "injectFn": {
      "title": "inject() Function",
      "navSubtitle": "Modern DI",
      "tag": "DX",
      "description": "Constructor injection requires listing all dependencies as constructor parameters — verbose and hard to refactor. The <code>inject()</code> function declares dependencies as class fields, is easier to read, and also works in standalone functions and composable helpers outside the class.",
      "panels": {
        "anti": "Constructor injection",
        "best": "inject() at field level"
      }
    },
    "routeInputBinding": {
      "title": "Route Input Binding",
      "navSubtitle": "withComponentInputBinding",
      "tag": "DX",
      "description": "Reading route parameters via <code>ActivatedRoute.snapshot</code> is imperative and verbose. With <code>withComponentInputBinding()</code> enabled in the router config, Angular binds route params, query params, and resolve data directly to <code>input()</code> signals — zero boilerplate.",
      "panels": {
        "anti": "ActivatedRoute.snapshot",
        "best": "withComponentInputBinding() + input()"
      }
    },
    "bootstrapApp": {
      "title": "bootstrapApplication()",
      "navSubtitle": "No NgModule",
      "tag": "DX",
      "description": "The classic <code>AppModule</code> with <code>@NgModule({ bootstrap })</code> is boilerplate that serves no purpose in an all-standalone app. <code>bootstrapApplication()</code> bootstraps the root component directly with a flat providers array — no module wrapper needed.",
      "panels": {
        "anti": "AppModule with @NgModule",
        "best": "bootstrapApplication()"
      }
    },
    "rxjsFlattening": {
      "title": "RxJS Flattening Operators",
      "navSubtitle": "switchMap / mergeMap / concatMap",
      "tag": "RxJS",
      "description": "Choosing the wrong flattening operator silently breaks behavior. <strong>switchMap</strong> cancels the previous inner observable — ideal for search, catastrophic for saves. <strong>concatMap</strong> queues emissions — correct for ordered writes. <strong>mergeMap</strong> runs concurrently — good for parallel reads.",
      "panels": {
        "anti": "switchMap for writes (cancels saves)",
        "middle": "mergeMap — concurrent reads",
        "best": "Right operator for the right job"
      }
    },
    "combineLatest": {
      "title": "combineLatest / withLatestFrom",
      "navSubtitle": "Combining streams",
      "tag": "RxJS",
      "description": "Nesting <code>subscribe()</code> inside <code>subscribe()</code> creates callback hell, memory leaks, and impossible cleanup. <code>combineLatest()</code> emits whenever any source emits. <code>withLatestFrom()</code> samples a secondary stream only when the primary emits — both are flat and composable.",
      "panels": {
        "anti": "Nested subscribe()",
        "best": "combineLatest() / withLatestFrom()"
      }
    },
    "higherOrderRxjs": {
      "title": "Higher-Order Observables",
      "navSubtitle": "Flat pipelines",
      "tag": "RxJS",
      "description": "Subscribing inside a <code>subscribe()</code> callback creates deeply nested, unmanageable code with no way to cancel inner subscriptions when the outer completes. Higher-order operators like <code>switchMap</code> flatten the stream into a single subscription that is automatically managed.",
      "panels": {
        "anti": "Nested subscribe() inside subscribe()",
        "best": "switchMap() flat pipeline"
      }
    },
    "formsStrategy": {
      "title": "Forms Strategy",
      "navSubtitle": "Best practices (bonus)",
      "tag": "Bonus",
      "description": "Use <strong>Reactive Forms</strong> for complex, dynamic, or programmatically validated forms - validation lives in TypeScript, is easy to test, and composes well. Use <strong>Template-driven</strong> (<code>ngModel</code>) only for simple, isolated inputs where no cross-field validation or dynamic control generation is needed.",
      "panels": {
        "anti": "Template-driven for complex forms",
        "best": "Reactive form"
      },
      "demo": {
        "label": "Live reactive form",
        "success": "Form submitted successfully - validation passed.",
        "submit": "Submit",
        "fields": {
          "email": {
            "label": "Email",
            "placeholder": "you@example.com",
            "errors": {
              "required": "Email is required.",
              "invalid": "Enter a valid email address."
            }
          },
          "username": {
            "label": "Username",
            "placeholder": "min. 3 characters",
            "errors": {
              "required": "Username is required.",
              "minLength": "Username must be at least 3 characters."
            }
          }
        }
      }
    }
  }
}
